# ── Supabase ────────────────────────────────────────────────────────────────
# Se llenan cuando exista el proyecto (aún no creado).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Solo servidor. Nunca prefijar con NEXT_PUBLIC_ (§27).
SUPABASE_SECRET_KEY=

# ── Proveedor de OCR / IA ───────────────────────────────────────────────────
# Enfoque A recomendado (PROJECT_CONTEXT.md §5): modelo multimodal con visión,
# llamado siempre desde el servidor. Pendiente de la validación técnica con
# documentos reales antes de construir el pipeline.
ANTHROPIC_API_KEY=
